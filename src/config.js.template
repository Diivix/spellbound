var config = {
    server: {
        http_port: 3080
    },
    db: {
        connection: "server:port/databasename",
        user: "account_name",
        password: "password"
    }
};

//module.exports = config;
export default config